import toDate from "./toDate";
import toNumber from "./toNumber";
import toString from "./toString";
import trim from "./trim";

export { toDate, toNumber, trim, toString };

export default {
    toDate,
    toNumber,
    trim,
    toString
};